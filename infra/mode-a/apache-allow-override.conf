# Test-only: let WordPress's generated .htaccess take effect so pretty
# permalinks (and therefore /wp-json/... without the ?rest_route= fallback)
# work in the Mode A verification stack. The official wordpress:apache image
# does not guarantee AllowOverride All out of the box.
<Directory /var/www/html>
    AllowOverride All
</Directory>
